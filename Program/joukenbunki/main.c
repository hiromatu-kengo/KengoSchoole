#include<stdio.h>
#include<windows.h>

int main()
{
	SetConsoleOutputCP(65001);	
	/*
	int num = 95;

	//if(条件）
	//{
	//　条件が合っていたら実行されるプログラム
	//}

	//== 左辺と右辺の値が同じならば条件を満たす

	if (num == 95)
	{
		printf("numは95です\n");
	}

	if (num == 100)
	{
		printf("numは100です\n");
	}

	// >　左辺のほうが大きい場合条件を満たす
	if (num > 80)
	{
		printf("numは80より大きい\n");
	}
	if (num > 95)
	{
		printf("numは95より大きい\n");
	}

	// >=　左辺の値が右辺の値以上に場合を満たす
	if (num >= 95)
	{
		printf("numは95以上\n");
	}
	if (num >= 120)
	{
		printf("numは95以下\n");
	}

	//<左辺の値のほうが小さい場合の条件を満たす
	if (num < 200)
	{
		printf("numは200より小さい\n");
	}
	//<=左辺の値が右辺の値以下の場合条件を満たす
	if (num <= 95)
	{
		printf("numは95以下\n");
	}

	// != 左辺の値と右辺の値等しくない場合条件を満たす
	if (num != 100)
	{
		printf("numは100ではない\n");
	}
	*/

	return 0;
}